* TIPS * :
1. Add into dependencies: npm --save <package_name>
2. Add into devDependencies: npm (--save-dev/-D) <package_name>

* STEPS * :
1. npm install
2. npm run typings install (if typings folder does not exist)

